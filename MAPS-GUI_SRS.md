# מפרט דרישות תוכנה (SRS) — מערכת מפה אינטראקטיבית maps-gui

**גרסת מסמך:** 1.0 | **תאריך:** 07/08/2026 | **פרויקט:** maps-gui | **מיקום:** `d:\PY-IS\maps-gui`
**נסרק ונכתב אוטומטית ע"י Claude Code מתוך קריאת קוד המקור בפועל (לא מתוך תיעוד קיים).**

מסמך משלים ל-[MAPS-GUI_README.md](MAPS-GUI_README.md) (מדריך טכני/תפעולי). מסמך זה הוא **מפרט דרישות** — מה המערכת אמורה לעשות, עבור מי, ובאילו אילוצים — ברמת פירוט הניתנת למעקב (traceable) לפי מזהי דרישה.

---

## תוכן עניינים

1. [מבוא](#1-מבוא)
2. [תיאור כללי של המערכת](#2-תיאור-כללי-של-המערכת)
3. [דרישות פונקציונליות — אפליקציית Desktop (PyQt5)](#3-דרישות-פונקציונליות--אפליקציית-desktop-pyqt5)
4. [דרישות פונקציונליות — שרתי Backend (Flask)](#4-דרישות-פונקציונליות--שרתי-backend-flask)
5. [דרישות פונקציונליות — אפליקציית Android (Flutter)](#5-דרישות-פונקציונליות--אפליקציית-android-flutter)
6. [ממשקים חיצוניים (External Interfaces)](#6-ממשקים-חיצוניים-external-interfaces)
7. [נתונים, תצורה והתמדה (Configuration & Persistence)](#7-נתונים-תצורה-והתמדה-configuration--persistence)
8. [דרישות לא-פונקציונליות (NFR)](#8-דרישות-לא-פונקציונליות-nfr)
9. [פערים, קוד מת ובעיות ידועות שהתגלו בסריקה](#9-פערים-קוד-מת-ובעיות-ידועות-שהתגלו-בסריקה)
10. [תהליך Build ופריסה](#10-תהליך-build-ופריסה)
11. [נספח: מיפוי קבצים ↔ דרישות](#11-נספח-מיפוי-קבצים--דרישות)

---

## 1. מבוא

### 1.1 מטרת המסמך
לתעד באופן פורמלי ומקיף את דרישות התוכנה של מערכת maps-gui, כפי שהן **קיימות בפועל בקוד** (reverse-engineered), לצורך: (א) בסיס ידע למפתחים/כלי AI העובדים על הקוד, (ב) זיהוי פערים/דרישות לא-ממומשות/קוד מת, (ג) בסיס להרחבות עתידיות.

### 1.2 היקף המוצר
מערכת דו-פלטפורמית להצגת מידע גיאוגרפי-מטאורולוגי על גבי מפה אינטראקטיבית:
- **צד Desktop (Windows):** אפליקציית PyQt5 + QWebEngineView המציגה מפת Leaflet.js, ומפעילה 3 שרתי Flask מקומיים כ-proxy לשירותי API חיצוניים.
- **צד Android:** אפליקציית Flutter עצמאית (flutter_map) הפונה ישירות ל-API-ים חיצוניים (ללא תלות בשרתי ה-Desktop, פרט לניסיון fallback ראשון אופציונלי).
- **שרתי Backend:** שלושה שרתי Flask נפרדים (geo/weather/flight) המשמשים בעיקר את צד ה-Desktop.

שתי הפלטפורמות **אינן משתפות קוד** (אין monorepo משותף של לוגיקה) — הן שתי מימושים מקבילים של אותו רעיון מוצרי, שכל אחד מדבר ישירות אל ספקי המידע החיצוניים (או, ב-Desktop, דרך שרתי ה-proxy המקומיים).

### 1.3 הגדרות וקיצורים
| מונח | פירוש |
|---|---|
| LOS | Line of Sight — קו ראייה, חישוב נראות בין שתי נקודות תוך התחשבות בעקמומיות כדור הארץ ורפרקציה אטמוספרית |
| Grid Point | נקודת דגימה על רשת גיאוגרפית (lat, lon, value) — עבור גובה או טמפרטורה |
| FR24 | FlightRadar24 — שירות מסחרי למעקב טיסות |
| ICAO/IATA | קודי זיהוי חברות תעופה (3 אותיות / 2 אותיות בהתאמה) |
| NFR | Non-Functional Requirement — דרישה לא-פונקציונלית |
| MRU | Most Recently Used — סדר "נעשה בו שימוש לאחרונה" (להיסטוריית ערים/טיסות) |

### 1.4 סקירת המסמך
פרקים 3–5 מפרטים דרישות פונקציונליות לפי רכיב (Desktop / Backend / Android), עם מזהים ניתנים למעקב (`FR-D-###`, `FR-S-###`, `FR-A-###`). פרק 6 מרכז את כל ממשקי ה-API החיצוניים. פרקים 7–8 מתעדים תצורה ו-NFRs. פרק 9 הוא רשימת ממצאים קונקרטיים (קוד מת, פערי אבטחה, חוסר עקביות) שעלו תוך כדי הסריקה — לא "רצוי לתקן" גנרי, אלא ציטוט מדויק של הבעיה במקור.

---

## 2. תיאור כללי של המערכת

### 2.1 ארכיטקטורה
```
                    ┌─────────────────────────────────┐
                    │   Desktop App (main.py, PyQt5)   │
                    │   QWebEngineView → map.html      │
                    │   (Leaflet.js, נוצר ע"י MAP.py)  │
                    └───────────┬───────────────────────┘
                                │ subprocess.Popen
              ┌─────────────────┼─────────────────┐
              ▼                 ▼                 ▼
      geo_server.py     weather_server.py   flight_server.py
      port 5003         port 5002           port 5004
      (Google Geocode)  (OpenWeather,       (FlightRadar24API,
                         Open-Meteo elev/    IATA→ICAO map)
                         forecast/LOS)
              │                 │                 │
              ▼                 ▼                 ▼
      Google Geocoding   OpenWeatherMap,     FlightRadar24
      API                Open-Meteo API      (unofficial)


                    ┌─────────────────────────────────┐
                    │  Android App (Flutter, עצמאי)    │
                    │  flutter_map + Provider/State    │
                    └───────────┬───────────────────────┘
                                │ HTTP ישיר (ללא שרת ביניים, ברירת מחדל)
              ┌─────────────────┼─────────────────┬──────────────┐
              ▼                 ▼                 ▼              ▼
      Open-Meteo         Nominatim          OpenSky →       שרת Desktop
      (elevation,        (geocoding עיר)    FR24 Gold       (fallback ראשון,
      forecast, LOS                         (מעקב טיסות,    לרוב לא זמין —
      מחושב on-device)                      fallback chain) ראה §9)
```

### 2.2 פונקציות עיקריות (טבלת השוואה)

| פיצ'ר | Desktop | Android |
|---|---|---|
| מפה אינטראקטיבית | Leaflet.js בתוך WebView | flutter_map (native) |
| שכבת גבהים | רשת נקודות + heat/grid/dots, מקור: Open-Meteo דרך שרת מקומי | זהה, ישירות מ-Open-Meteo, נרנדר כתמונה on-device |
| שכבת טמפרטורות | רשת נקודות + heat/grid/dots, מקור: Open-Meteo דרך שרת מקומי | זהה, ישירות מ-Open-Meteo |
| שכבת "חום ידני" (נקודות שהמשתמש מסמן) | כן (Leaflet.heat, ללא קריאת שרת) | כן (Gaussian kernel on-device, ללא קריאת שרת) |
| מזג אוויר לפי עיר/נ.צ | כן (OpenWeather, דרך שרת מקומי) | כן (Open-Meteo, ישיר) |
| חיפוש עיר עם גבולות | כן (Google Geocoding, boundary) | כן (Nominatim/OSM, bounding box) |
| מעקב מסלולי טיסה | כן (FlightRadar24API, שרת מקומי בלבד) | כן (3 fallback tiers: שרת מקומי → OpenSky → FR24) |
| כלי מדידת מרחק (Ruler) | כן (Haversine, קליק-רב-נקודתי) | כן (זהה בעקרון, ממומש בנפרד) |
| קו ראייה (LOS) | כן (מחושב בשרת `/los`, פרופיל גובה מצויר בקנבס) | כן (מחושב **on-device**, ללא שרת ייעודי) |
| GPS / מיקום עצמי | לא קיים | כן (`geolocator`, מתווסף כ"סיכה") |
| היסטוריית ערים/טיסות | כן (QSettings, 10/15 פריטים) | כן (`shared_preferences`, 10 פריטים לכל אחד) |
| תמיכת RTL/עברית | מלאה | מלאה (`Locale('he','IL')` כפוי) |
| ערכת צבעים | כהה — Catppuccin Mocha (הן ב-Qt GUI והן ב-HTML/JS) | כהה — Material 3, `ThemeMode.dark` כפוי |
| שכבות רקע (בסיס מפה) | טופו / רחובות / לוויין (3 קבועות) | OSM רגיל / ESRI לוויין / CartoDB כהה (dropdown) |

### 2.3 משתמשי קצה ומאפייניהם
- משתמש יחיד (single-user), ללא ניהול משתמשים, ללא login, ללא הרשאות מדורגות.
- דובר/ת עברית — כל הממשק, ההודעות, השגיאות בעברית בלבד; אין i18n/תמיכה בשפות נוספות.
- Desktop מיועד להרצה מקומית על מחשב Windows יחיד של המשתמש (לא שרת רב-משתמשים).
- Android מיועד למכשיר אישי; אופציונלי להתחבר לאותה רשת Wi-Fi כמו ה-Desktop לצורך fallback במעקב טיסות (כיום לא מחובר בפועל — ר' §9).

### 2.4 סביבת הפעלה
- **Desktop:** Windows, Python 3.14 (venv ב-`.venv/`), PyQt5 5.15.11 + PyQtWebEngine 5.15.7.
- **Android:** Flutter SDK מוצמד לגרסה `3.41.9-stable`, Java 17, Android SDK `build-tools;35.0.0` + `platforms;android-35`, `minSdk`/`targetSdk` נגזרים מגרסת ה-Flutter plugin (לא קבועים ידנית).
- **רשת:** כל הרכיבים (Desktop ו-Android) דורשים גישה לאינטרנט לצורך קריאות API חיצוניות. תעבורת HTTP לא-מוצפנת (`cleartext`) מותרת באנדרואיד לצורך תקשורת עם שרת ה-Desktop המקומי.

### 2.5 אילוצים כלליים
- ללא בסיס נתונים — כל האחסון הוא קבצים מקומיים (`.env`, `*.log`, `*.csv`, `QSettings`/`shared_preferences`).
- ללא מנגנון אימות/הרשאות (auth) בשום שכבה — כל שרת Flask מקומי חשוף ללא הגנה (מוגן רק ע"י היותו `localhost`/רשת מקומית).
- הפרויקט אינו נמצא תחת git (ראה `git status`/היסטוריה בפועל בזמן כתיבת מסמך זה — יש כן `.git` בפרויקט הנוכחי; זה סותר תיעוד קודם שאומר "ללא גיט", ר' §9 לפרטים).
- מפתחות API (OpenWeather, Google, FlightRadar24) מוטמעים כערכים אמיתיים בקובץ `.env` בשורש הפרויקט וכן, עבור FR24, כמחרוזת קשיחה בקוד ה-Android (`api_service.dart`).

### 2.6 הנחות ותלויות
- זמינות והתנהגות תקינה של שירותי צד ג': OpenWeatherMap, Google Geocoding, Open-Meteo, Nominatim/OSM, OpenSky Network, FlightRadar24 (הבלתי-רשמי).
- כל השירותים החיצוניים חינמיים/פרימיום עם הגבלות קצב (rate limit) — המערכת בנויה סביב ההנחה שהגבלות אלה קיימות (chunking, delays, retries — ר' §4, §5).

---

## 3. דרישות פונקציונליות — אפליקציית Desktop (PyQt5)

**קובץ מקור:** `main.py` (חלון ראשי) + `MAP.py` (מחולל `map.html`) + `apipyqt.py` (עזרי API, לא בשימוש בפועל ע"י `main.py` הראשי).

### 3.1 חלון ראשי ומבנה כללי
- **FR-D-001:** האפליקציה תציג חלון יחיד בכותרת "ניהול מערכת מפה אינטראקטיבית", גודל מינימלי 900×600, גודל פתיחה 1280×750, ממורכז במסך הראשי.
- **FR-D-002:** החלון יחולק ל-2 חלוניות ע"י `QSplitter` אופקי: מפה (שמאל, גמישה) ופאנל כפתורים (ימין, רוחב מינימלי 165px, לא ניתן לכיווץ).
- **FR-D-003:** שורת סטטוס תציג "מוכן" בעת עלייה, ותציג כל פעולת יומן במשך 4000ms.
- **FR-D-004:** ערכת נושא כהה קבועה (Catppuccin Mocha) על פני כל הרכיבים — אין אפשרות מיתוג ערכת נושא בזמן ריצה.

### 3.2 תצוגת המפה
- **FR-D-010:** המפה תוצג באמצעות `QWebEngineView` הטוען קובץ `map.html` מקומי (`QUrl.fromLocalFile`).
- **FR-D-011:** לחיצה על "יצירת / איפוס מפה" תיצור בפעם הראשונה קובץ `map.html` (קריאה ל-`create_map()`) ותטען אותו; בלחיצות חוזרות תאפס את מצב המפה דרך קריאת JS (`resetMapState()`) **מבלי ליצור מחדש את הקובץ**.
- **FR-D-012:** שגיאות JavaScript בדף (כולל `window.onerror`) יילכדו ויירשמו ביומן הצד-Python (`JS console`/`JS error`), כולל מקור/שורה/עמודה.
- **FR-D-013:** תקשורת JS→Python תתבצע באמצעות מנגנון sentinel-string על `document.title` (לא QWebChannel) — ר' רשימת סמנטיקות בטבלה:

| Sentinel (title) | משמעות בצד Python |
|---|---|
| `__jserror__:<msg> @ <src>:<line>:<col>` | שגיאת JS — נרשם כשגיאה |
| `__heatmap_computing__` / `__heatmap_loaded__` / `__heatmap_error__` | מצב טעינת שכבת טמפרטורה (rectangle-select) |
| `__heatmap_edit_error__` | שגיאה בעדכון בחירה קיימת |
| `__elev_computing__` / `__elev_loaded__` / `__elev_error__` | מצב טעינת שכבת גבהים |
| `__los_loading__` / `__los_loaded__` / `__los_error__` | מצב חישוב קו ראייה |

### 3.3 ניהול שרתי Backend (subprocess)
- **FR-D-020:** בעת אתחול, האפליקציה תבצע ניקוי "zombie processes" על פורטים 5002/5003/5004 (`netstat -ano` + `taskkill /F /PID`), לפני יצירת ה-UI.
- **FR-D-021:** כפתור יחיד ("הפעל שרתים"/"עצור שרתים") יפעיל/יעצור 3 תהליכי Python נפרדים כ-subprocess: `geo_server.py` (5003), `weather_server.py` (5002), `flight_server.py` (5004).
- **FR-D-022:** בעת הפעלה מוצלחת, יעודכנו 2 "נורות סטטוס" (geo, weather בלבד — **אין נורית לשרת הטיסות**), ה-JS global `window.serversRunning` יוגדר ל-`true`, וכפתורים תלויי-שרת יופעלו.
- **FR-D-023:** עצירה תבצע `terminate()` (SIGTERM) על כל תהליך פעיל, תאפס נוריות לכבוי, תגדיר `serversRunning=false`, ותנטרל את הכפתורים תלויי-השרת.
- **FR-D-024:** בעת סגירת האפליקציה (`closeEvent`), כל השרתים ייעצרו אוטומטית לפני סגירה סופית.

### 3.4 יומן פעולות (Log) והתראות
- **FR-D-030:** כל פעולה משמעותית תירשם ל: (א) רשימת יומן בזיכרון (מוגבלת ל-500 שורות אחרונות), (ב) קובץ `app_combined.log` (`logging.INFO`), (ג) שורת הסטטוס.
- **FR-D-031:** שורות יומן יסומנו בצבע לפי תוצאה: הצלחה (ירוק `#a6e3a1`), כישלון (אדום `#f38ba8`), ניטרלי (`#cdd6f4`).
- **FR-D-032:** פאנל היומן ניתן להצגה/הסתרה ע"י כפתור-פעמון; כאשר מוסתר, פעולות חדשות יגדילו תגית מספרית (badge) אדומה מעל הפעמון; פתיחת הפאנל תאפס את המונה.
- **FR-D-033:** תתאפשר שמירת היומן לקובץ טקסט (`QFileDialog`) וניקוי היומן המוצג.

### 3.5 קלט מיקום — קואורדינטות
- **FR-D-040:** טאב ייעודי (מוסתר כברירת מחדל, מוצג ע"י כפתור "דקור נ.צ") יאפשר הזנת קו-רוחב (`-90..90`) וקו-אורך (`-180..180`) ב-6 ספרות עשרוניות.
- **FR-D-041:** שינוי ערך בכל אחד משדות הקואורדינטות יפעיל **debounce של 600ms** לפני שליחת בקשת מזג אוויר אוטומטית (מונע הצפת בקשות בעת גלילת spinbox).
- **FR-D-042:** כפתור "שלח" ישלח מיידית (ללא debounce) ויתעד את הבקשה לקובץ `coordinates_log.txt`.
- **FR-D-043 (ולידציה):** ערכי `lat==0` וגם/או `lon==0` ייחשבו "לא הוזן קלט תקין" ויידחו — ר' הערת פער ב-§9 (0,0 היא נקודה גיאוגרפית תקפה).

### 3.6 קלט מיקום — עיר
- **FR-D-050:** טאב "עיר" יציג תיבת קלט ניתנת לעריכה עם היסטוריה (`QComboBox`), Placeholder "הכנס שם עיר".
- **FR-D-051:** הזנת שם עיר (Enter או כפתור "שלח") תישלח מקודדת (URL-encoded) לשרת מזג האוויר, עם רישום לקובץ `city_log.txt`.
- **FR-D-052:** היסטוריית ערים תישמר עד 10 פריטים אחרונים (MRU) דרך `QSettings("MapApp","MapGUI")`, מפתח `city_history`.

### 3.7 הצגת מזג אוויר על המפה
- **FR-D-060:** בקשת מזג אוויר מוצלחת תוסיף סמן (`L.marker`) עם חלונית מידע (lat/lon, תיאור מזג אוויר, טמפרטורה, גובה).
- **FR-D-061 (Cache):** תוצאות ייקשרו ב-cache בזיכרון לפי מפתח `(round(lat,3), round(lon,3))` — כלומר טולרנס של כ-100 מטר נחשב "אותה נקודה". שם השדה כולל timestamp, אך **אין אכיפת תפוגה (TTL)** בפועל — ר' §9.
- **FR-D-062:** אם התגובה כוללת `boundary` (גבולות עיר), תצויר מלבן אדום שקוף-למחצה מעל תחום העיר.
- **FR-D-063:** גובה הנקודה יובא בבקשה נפרדת ל-`/elevation`; כישלון בקבלת גובה יוצג כ-"לא זמין" ולא יחסום את הצגת שאר הנתונים.
- **FR-D-064:** כפתור "נקה סמנים" ימחק את כל הסמנים שנוספו למפה.

### 3.8 שכבת טמפרטורה (heatmap אינטראקטיבי)
- **FR-D-070:** כפתור "שכבת חום" (checkable) יפעיל מצב "גרירת מלבן על המפה" לבחירת אזור; רזולוציית דגימה בסיסית 5 ק"מ, עם הגבלת מספר נקודות מרבי לביצועים (ר' §5, `/temp_grid`).
- **FR-D-071:** לאחר טעינה, ניתן להחליף בין 3 מצבי תצוגה: "חום" (heatmap גרדיאנט רציף מצויר על canvas), "גריד" (עיגולים גדולים), "נקודות" (עיגולים קטנים) — ללא צורך בבקשת שרת חוזרת (נתונים כבר בזיכרון).
- **FR-D-072:** מחוון שקיפות (0–100, ברירת מחדל 60) ישלוט על שקיפות שכבת ה-heat.
- **FR-D-073:** מלבן הבחירה כולל 4 "ידיות" גרירה בפינות לעריכת האזור לאחר הבחירה הראשונית, עם רענון אוטומטי של הנתונים בסיום גרירה.
- **FR-D-074:** מצב נפרד "בחר נקודות" יאפשר סימון ידני של נקודות בודדות על שכבת heat נפרדת (Leaflet.heat), ללא קריאת שרת.
- **FR-D-075:** חריגה ממכסת API (429) תוצג למשתמש כהתראה מפורשת "מכסת API יומית נוצלה — נסה שוב מחר".

### 3.9 שכבת גבהים
- **FR-D-080:** זהה במבנה לשכבת הטמפרטורה (בחירת מלבן, גריד 10×10=100 נקודות, מצבי heat/grid/dots), עם גרדיאנט כחול(נמוך)→אדום(גבוה).
- **FR-D-081:** לחיצה על המפה במצב שכבת-גבהים פעילה תציג popup עם גובה הנקודה הקרובה ביותר שנדגמה (חיפוש שכן-קרוב לפי מרחק בריבוע).

### 3.10 כלי מדידת מרחק (Ruler)
- **FR-D-090:** מצב checkable "📏 מדד מרחק" יאפשר סימון נקודות רצופות בקליק; כל קטע יסומן בקו ירוק מקווקו עם תווית מרחק; popup מצטבר יציג מרחק כולל בק"מ ובמייל ימי (NM).
- **FR-D-091:** "נקה מדידה" תסיר את כל הקווים/הנקודות/ה-popup ותאפס את סמן העכבר למצב רגיל.

### 3.11 קו ראייה (LOS)
- **FR-D-100:** מצב "👁" יאפשר בחירת זוגות נקודות (תצפית→יעד) ברצף; כל זוג ייצור "session" עצמאי עם צבע ייחודי מתוך פלטת 6 צבעים.
- **FR-D-101:** לכל session תחושב, בצד שרת (`GET /los`), תוצאת נראות מבוססת פרופיל גובה (ר' §4.5), ותצויר על המפה כקו ירוק (נראה)/אדום (חסום) לפי מקטעים.
- **FR-D-102:** פאנל תחתון ייעודי יציג, לכל session פעיל, גרף פרופיל גובה + קו ראייה (canvas), מרחק כולל, ומרחק לחסימה הראשונה אם קיימת.
- **FR-D-103:** כל session ניתן להסרה עצמאית ("✕"); קיים כפתור גורף "נקה הכל" שגם מכבה את מצב ה-LOS.

### 3.12 מעקב טיסות
- **FR-D-110:** שדה קלט טיסה (placeholder "מס' טיסה — LY001") עם היסטוריה (עד 15 פריטים, MRU, `QSettings` מפתח `flight_history`); Enter או כפתור "הצג מסלול" יפעילו את החיפוש.
- **FR-D-111:** הבקשה תישלח ל-`GET /flight_route` עם **timeout מוארך של 35 שניות** (מתועד כנדרש עקב זמני תגובה ארוכים של FR24).
- **FR-D-112:** בהצלחה: יצויר מסלול (polyline), סמן מטוס מסתובב לפי heading, וסמני שדות תעופה למקור/יעד; המפה תתמקד אוטומטית לגבולות המסלול.
- **FR-D-113:** Timeout ספציפי (`requests.exceptions.Timeout`) יוצג כהודעה "פסק זמן בחיפוש הטיסה — נסה שנית."; הכפתור יינעל בזמן הבקשה וישוחרר תמיד (`finally`) בסיומה.
- **FR-D-114:** "נקה" תסיר את המסלול המוצג מהמפה.

### 3.13 שכבות רקע (בסיס מפה)
- **FR-D-120:** בקר שכבות ייעודי (top-right) יאפשר מעבר בין 3 שכבות בסיס: טופו (OpenTopoMap, ברירת מחדל), רחובות (OSM), לוויין (Esri World Imagery).

---

## 4. דרישות פונקציונליות — שרתי Backend (Flask)

כל שרת: Flask + `flask-cors` (CORS גלובלי), הרצה נפרדת כתהליך OS, `debug=True` בכל ה-3 (ר' הערת אבטחה ב-§9).

### 4.1 `geo_server.py` — פורט 5003
- **FR-S-001:** `GET /geo_data?location=<עיר>` — פרמטר `location` חובה (400 אם חסר).
- **FR-S-002:** קריאה ל-Google Geocoding API עם `timeout=5`; מחזיר `{address, latitude, longitude}` מהתוצאה הראשונה בלבד.
- **FR-S-003:** מיפוי שגיאות: `404` (מיקום לא נמצא), `408` (timeout), `500` (שגיאת חיבור אחרת) — כולם עם הודעה בעברית.

### 4.2 `weather_server.py` — פורט 5002 (המכיל בפועל את רוב הנתיבים)
- **FR-S-010:** `GET /weather?region=<עיר>` **או** `?lat=&lon=` (לפחות אחד חובה, 400 אחרת) — קריאה ל-OpenWeatherMap (`units=metric`, `lang=he`); כאשר מסופק `region`, מבוצעת גם קריאה נפרדת ל-Google Geocoding לשליפת גבולות עיר (`boundary`, יהיה `null` בבקשות lat/lon).
- **FR-S-011:** תגובה: `{region, weather, temperature, latitude, longitude, boundary}`; שגיאות ממופות לפי קוד הסטטוס שהוחזר מ-OpenWeatherMap, ו-`500` לכל חריגה אחרת. **אין timeout מוגדר** על קריאות ה-HTTP בנתיב זה (חריג ביחס לשאר הנתיבים).
- **FR-S-020:** `GET /heatmap_data` — ללא פרמטרים; קורא `weather_data.csv` (אם קיים ותקין) או מחזיר 3 נקודות ברירת מחדל קבועות (ת"א, ירושלים, אילת) — **אף פעם לא מחזיר שגיאה**.
- **FR-S-030:** `POST /elevation` — גוף `{locations:[{latitude,longitude}]}`; חיתוך אוטומטי ל-100 נקודות ראשונות (מגבלת Open-Meteo); קריאה ל-Open-Meteo Elevation API עם `timeout=30` ו**עד 3 ניסיונות חוזרים** (backoff `0.5*(ניסיון+1)` שניות) על timeout/שגיאת חיבור; `429` ממופה ל-429 עם `rate_limited:true`.
- **FR-S-040:** `POST /temp_grid` — גוף `{southwest:{lat,lng}, northeast:{lat,lng}}` (שניהם חובה); חישוב רשת אדפטיבי: בסיס 5 ק"מ, מוגבל ל-**500 נקודות מרבי** (`MAX_POINTS`), עם הגדלת המרווח אוטומטית כשהאזור גדול מדי; שליחה ל-Open-Meteo Forecast API ב-**batches של 30 נקודות**, עם **השהיה של 500ms בין batches** ו-**3 ניסיונות חוזרים** עם backoff אקספוננציאלי (2s/4s); `429` מבטל את כל הבקשה (לא רק ה-batch); אזור קטן מדי (רשת ריקה) → `400`.
- **FR-S-050:** `GET /los?lat1=&lon1=&lat2=&lon2=` — כל 4 הפרמטרים חובה (400 אחרת). קבועים: רדיוס כדה"א 6,371,000 מ', מקדם רפרקציה `k=0.13`, גובה משקיף 11 מ' מעל הקרקע, מרווח דגימה 5 ק"מ, מרחק מרבי נתמך 500 ק"מ. דוגם עד 100 נקודות לאורך הקשת הגדולה (great-circle), שולף גבהים בקריאה יחידה (`timeout=30`, **ללא retry** — שונה מ-`/elevation`). מחזיר `{points:[{lat,lon,dist_km,elevation,los_h,visible}], total_km, observer_elev, observer_h, first_block_km, all_visible}`.

### 4.3 `flight_server.py` — פורט 5004
- **FR-S-060:** אסטרטגיית אימות מדורגת מקובץ `.env` (סדר עדיפות): (1) `FR24_TOKEN` — הזרקת cookie ישירות (עוקף Cloudflare/login), (2) `FR24_USER`+`FR24_PASS` — login רגיל (ממשיך במצב "ללא trail" בכישלון), (3) ללא אימות — מצב "מיקום נוכחי בלבד".
- **FR-S-061:** `GET /flight_route?flight=<מספר>` — פרמטר חובה (400 אחרת). אסטרטגיית חיפוש רב-שכבתית: (1) חיפוש ממוקד לפי קידומת חברת תעופה שהומרה מ-IATA ל-ICAO (מילון קשיח ~50 חברות), (2) אם לא נמצא — חיפוש ברשימה הגלובלית (~1500 טיסות), (3) אם לא נמצא — `fr_api.search()` + `get_flight_details()` ישירות, (4) אחרת — `404`.
- **FR-S-062:** מסלול (`trail`) מוגבל ל-400 הנקודות האחרונות (הגבלת גודל payload); תגובה כוללת callsign, נמל מוצא/יעד (IATA+שם), דגם מטוס, חברה, גובה/מהירות/heading נוכחיים, ורשימת נקודות מסלול.
- **FR-S-063:** `GET /flight_search?q=` — autocomplete; דורש `len(q)>=2` (מונע שליפת כל הרשימה הגלובלית עבור שאילתה קצרה מדי); מחזיר עד 20 תוצאות; **לעולם לא מחזיר שגיאה** (מחזיר `[]` בכישלון) — ר' הערה ב-§9 לגבי אי-שימוש בפועל.

---

## 5. דרישות פונקציונליות — אפליקציית Android (Flutter)

**קבצי מקור:** `maps-gui-android-src/lib/**` (מקור קנוני המועתק ע"י `build_apk.ps1` אל `maps-gui-android/` בזמן build).

### 5.1 מסך ראשי ומצב אפליקציה
- **FR-A-001:** האפליקציה תיפתח ב-Locale כפוי `he_IL`, `ThemeMode.dark` כפוי (Material 3, seed color כחול `#1565C0`), ותמנע סיבוב "הפוך" (portrait-down).
- **FR-A-002:** מסך יחיד (`MapScreen`) עם `FlutterMap` במרכז, סרגל עליון צף מותאם-אישית (לא `AppBar` סטנדרטי), וסרגל תחתון (`BottomAppBar`) עם 3 כפתורי ניווט הפותחים bottom sheets: "שכבות", "מיקום", "טיסות".
- **FR-A-003:** מרכז התחלתי: `[31.5, 34.9]` (מרכז ישראל בקירוב), זום 7.

### 5.2 בחירת אזור (Selection)
- **FR-A-010:** מצב "בחירת אזור" ניתן להפעלה מהסרגל העליון (אייקון `crop_free`); ניתן לבחור אזור בשלוש דרכים שקולות: הקשה-הקשה (2 נקודות), גרירה (pan), או לחיצה-ארוכה+גרירה — תמיכה בכל שלוש הדרכים בו-זמנית לנוחות מגע.
- **FR-A-011:** אם קיימים כבר נתוני שכבה (גבהים/טמפרטורה) טעונים בעת ניסיון לבחור אזור חדש, תוצג דיאלוג אישור ("בחירה חדשה" / "נתונים טעונים לאזור הנוכחי. לאפס ולבחור אזור חדש?").
- **FR-A-012:** לאחר בחירה, מלבן הבחירה ניתן לעריכה חופשית ע"י 4 ידיות פינה נגררות (ללא צורך לחזור למצב בחירה), עם רענון-בקשה אוטומטי כל עוד השכבה טעונה.

### 5.3 שכבת גבהים
- **FR-A-020:** `ApiService.fetchElevationGrid` תבנה רשת אדפטיבית (בסיס 5 ק"מ, מקסימום 100 נקודות) ותשלח בבקשות **chunks של 50 נקודות** ל-Open-Meteo Elevation API (ללא השהיה בין chunks).
- **FR-A-021:** תוצאה תרונדר ל-heat-image (PNG 512×512, אינטרפולציה דו-ליניארית) עבור מצב "חום", או תוצג כ-`PolygonLayer`/`CircleLayer` במצבי "גריד"/"נקודות".
- **FR-A-022:** נתונים שכבר נטענו לאזור נוכחי לא יישלפו מחדש בהחלפת מצב תצוגה (heat/grid/dots) — רק ב-fetch ראשוני.
- **FR-A-023:** הקשה על המפה בעת שהשכבה פעילה ונתונים קיימים תציג כרטיס "גובה" (`_ElevationCard`) עם ערך הנקודה הקרובה (שכן-קרוב, ללא שורש ריבועי בחישוב מרחק).

### 5.4 שכבת טמפרטורה
- **FR-A-030:** `ApiService.fetchTemperatureGrid` — רשת עד 30 נקודות, **chunks של 20**, עם **השהיה של 200ms** לפני כל chunk מלבד הראשון (rate-limit), timeout 40 שניות ל-chunk.
- **FR-A-031:** התאמה לתגובת Open-Meteo שיכולה להיות מערך (מספר קואורדינטות) או אובייקט בודד (קואורדינטה יחידה) — טופל באופן שונה בכל מקרה.
- **FR-A-032:** אותם 3 מצבי תצוגה (heat/grid/dots) כמו שכבת הגבהים.

### 5.5 "חום ידני" (Manual Heat)
- **FR-A-040:** מצב נפרד (סרגל עליון, אייקון "אש") — הקשות על המפה מוסיפות נקודות המרונדרות מיידית ל-heat-image (Gaussian kernel density, `radiusDeg=0.02`, סף שקיפות מתחת ל-0.03 עוצמה) **ללא כל קריאת שרת**.

### 5.6 מיקום, סיכות וחיפוש עיר
- **FR-A-050:** כפתור "מיקום עצמי" יבקש הרשאת מיקום ברמת runtime (`geolocator`), עם טיפול נפרד בשלושת מצבי הכישלון: שירות כבוי, הרשאה נדחתה, הרשאה חסומה לצמיתות (עם הודעות עבריות ייעודיות לכל אחד); בהצלחה — הנקודה מתווספת כ"סיכה".
- **FR-A-051:** ניתן להוסיף סיכות ידנית ע"י הזנת lat/lon (ולידציה: טווח תקין + טיפול בפסיק כנקודה עשרונית); ניתן לנקות את כל הסיכות.
- **FR-A-052:** חיפוש עיר (`ApiService.geocodeCity`, Nominatim/OSM) יציג את גבולות העיר (bounding box) על המפה ויתמקד אליה; נשמר להיסטוריה (10 אחרונים).

### 5.7 מעקב טיסות (fallback chain תלת-שכבתי)
- **FR-A-060:** קלט מספר טיסה (רק אותיות/ספרות, capitalization אוטומטי), עם היסטוריה (10 אחרונים, `shared_preferences`).
- **FR-A-061 (סדר עדיפויות):**
  1. שרת Desktop מקומי (`http://{flightServerHost}:5004/flight_route`, timeout 12s) — **מותנה בהגדרת `flightServerHost` שאין לה כרגע כל דרך הגדרה מה-UI** (ר' §9).
  2. OpenSky Network (`states/all` לאיתור טיסה נוכחית + עד 3 ניסיונות `tracks/all` ב-offsets זמן שונים לשליפת מסלול היסטורי) — ללא צורך במפתח API.
  3. FlightRadar24 Gold API הבלתי-רשמי (`feed.js` + `clickhandler`) עם cookie token קשיח בקוד — כמוצא אחרון.
  - כל שכבה שנכשלת (ברשת או בפריסת הנתונים) "נבלעת" (swallowed) ועוברת אוטומטית לשכבה הבאה; כישלון כל 3 השכבות מציג שגיאה בעברית.
- **FR-A-062:** אם נמצאה מיקום נוכחי אך לא נמצא מסלול היסטורי (רק נקודה אחת), תוצג אזהרה רכה ("מיקום נוכחי בלבד — אין מסלול זמין") **תוך המשך הצגת הנתונים החלקיים** (לא נחסם).
- **FR-A-063:** תוצאה מוצלחת תמקד את המפה אוטומטית לגבולות המסלול (אם ≥2 נקודות) או לנקודה הבודדת (זום 8).

### 5.8 כלי מדידה (Ruler)
- **FR-A-070:** מצב checkable מהסרגל העליון; כל הקשה על המפה מוסיפה נקודה ומחשבת (Haversine) מרחק מצטבר מהנקודה הקודמת; כרטיס תחתון מציג מרחק כולל בק"מ ו-NM ומספר נקודות.

### 5.9 קו ראייה (LOS) — מחושב על המכשיר
- **FR-A-080:** בניגוד ל-Desktop, LOS ב-Android **מחושב כולו on-device** (`ApiService.fetchLos`) ולא דרך שרת ייעודי — שולף גבהים ב-chunks של 50 מ-Open-Meteo Elevation API, ומריץ את אותו אלגוריתם עקרונית (עקמומיות+רפרקציה, "horizon ratchet") כמו הגרסה בצד השרת של ה-Desktop (`/los`), אך כמימוש Dart עצמאי (לא שיתוף קוד).
- **FR-A-081:** כל צמד הקשות (תצפית→יעד) יוצר "session" עצמאי בצבע ייחודי (פלטת 6 צבעים, מחזורית); מספר sessions יכולים לדור בכתפיפה בו-זמנית.
- **FR-A-082:** פאנל תחתון גלילי אופקי מציג כרטיס פרופיל (גרף canvas: שטח פני שטח + קו ראייה בצבע ירוק/אדום למקטע נראה/חסום) לכל session, עם אפשרות סגירה עצמאית או ניקוי-הכל.

### 5.10 מזג אוויר לנקודה
- **FR-A-090:** הקשה על המפה (כברירת מחדל, כשאין מצב אחר פעיל) תבצע `ApiService.fetchPointWeather` (Open-Meteo Forecast) ותציג כרטיס עם טמפרטורה, לחות, רוח, ותיאור מזג אוויר (טבלת תרגום WMO code → עברית, 13 קודים ממופים, ברירת מחדל "לא ידוע").

### 5.11 שכבת רקע (בסיס מפה)
- **FR-A-100:** Dropdown לבחירת שכבת בסיס: "OSM רגיל", "ESRI לוויין", "CartoDB כהה" — משתנה מיידית ללא צורך ב-reload.

### 5.12 סדר עדיפות מחוות מגע על המפה
- **FR-A-110:** לחיצה על המפה תנותב לפי סדר עדיפות קבוע (הראשון שמתאים "זוכה"): חום ידני פעיל → LOS פעיל → Ruler פעיל → שכבת גבהים פעילה+יש נתונים → (ברירת מחדל) שליפת מזג אוויר לנקודה.

---

## 6. ממשקים חיצוניים (External Interfaces)

### 6.1 שירותי API חיצוניים בשימוש

| שירות | דורש מפתח | נצרך ע"י | שימוש |
|---|---|---|---|
| OpenWeatherMap | כן (`OPENWEATHER_API_KEY`) | `weather_server.py` (Desktop בלבד) | מזג אוויר עדכני לפי עיר/נ.צ |
| Google Geocoding API | כן (`GOOGLE_API_KEY`) | `geo_server.py`, `weather_server.py` (Desktop בלבד) | geocoding + גבולות עיר |
| Open-Meteo Elevation API | לא | `weather_server.py` (Desktop), `api_service.dart` (Android) | גבהים (גם לשכבת גבהים וגם לחישובי LOS) |
| Open-Meteo Forecast API | לא | `weather_server.py` (Desktop), `api_service.dart` (Android) | טמפרטורות רשת + מזג אוויר לנקודה (Android) |
| Nominatim (OpenStreetMap) | לא (נדרש User-Agent) | `api_service.dart` (Android בלבד) | חיפוש עיר + גבולות |
| OpenSky Network | לא | `api_service.dart` (Android בלבד) | מעקב טיסות — Tier 2 |
| FlightRadar24 (רשמי, ספריית Python) | כן (`FR24_TOKEN`/`FR24_USER`+`FR24_PASS`) | `flight_server.py` (Desktop) | מעקב טיסות (המקור העיקרי בצד Desktop) |
| FlightRadar24 (Gold API בלתי-רשמי, HTTP ישיר) | Cookie token קשיח בקוד | `api_service.dart` (Android בלבד) | מעקב טיסות — Tier 3 (fallback אחרון) |

### 6.2 ממשק Desktop↔Android (אופציונלי, לא פעיל כרגע)
- ה-Android יכול, עקרונית, לפנות לשרת ה-flight המקומי של ה-Desktop (`http://{flightServerHost}:5004`) כ-Tier 1 של מעקב טיסות, בתנאי שני המכשירים על אותה רשת Wi-Fi. **בפועל אין דרך להגדיר `flightServerHost` מה-UI** — ר' §9.

### 6.3 ממשקי HTTP פנימיים (Desktop, בין `main.py`/`map.html` לשרתים המקומיים)
ראה טבלת נתיבים מלאה בפרק 4 (`/geo_data`, `/weather`, `/heatmap_data`, `/elevation`, `/temp_grid`, `/los`, `/flight_route`, `/flight_search`).

---

## 7. נתונים, תצורה והתמדה (Configuration & Persistence)

### 7.1 קובץ `.env` (שורש הפרויקט, Desktop)
| משתנה | שימוש | הערה |
|---|---|---|
| `OPENWEATHER_API_KEY` | `weather_server.py` | ערך אמיתי בקובץ בפועל |
| `GOOGLE_API_KEY` | `geo_server.py`, `weather_server.py` | ערך אמיתי בקובץ בפועל |
| `ANOTHER_API_KEY` | לא בשימוש | placeholder (`your_other_api_key_here`), שמור |
| `FR24_USER` / `FR24_PASS` | `flight_server.py` (fallback login) | ערכים אמיתיים בקובץ בפועל |
| `FR24_TOKEN` | `flight_server.py` (שיטת אימות ראשית) | ערך אמיתי (cookie `_frPl`) |

### 7.2 התמדה בצד Desktop
- `QSettings("MapApp","MapGUI")`: `city_history` (עד 10), `flight_history` (עד 15).
- קבצים: `app_combined.log` (יומן ריצה), `coordinates_log.txt` (היסטוריית קלט קואורדינטות), `city_log.txt` (היסטוריית קלט ערים), `map.html` (מפה שנוצרת בריצה), `weather_data.csv`/`heatmap.html` (פלט כלי `weather_tool.py` העצמאי, לא קשור למפה החיה).

### 7.3 התמדה בצד Android
- `shared_preferences`: `city_search_history`, `flight_history` (עד 10 פריטים כל אחד).

### 7.4 קונפיגורציית Build (Android)
- `applicationId`/`namespace`: `com.mapsapp.maps_gui_android`.
- `minSdk`/`targetSdk`/`compileSdk`: נגזרים מ-Flutter plugin (לא קבועים ידנית); בסביבת ה-build הנוכחית — `platforms;android-35`, `build-tools;35.0.0`.
- Java/Kotlin: גרסה 17 (קבועה).
- גרסת אפליקציה: `1.0.0+1` (versionName `1.0.0`, versionCode `1`), נגזרת מ-`pubspec.yaml`.
- הרשאות (`AndroidManifest.xml`): `INTERNET`, `ACCESS_NETWORK_STATE`, `ACCESS_FINE_LOCATION`, `ACCESS_COARSE_LOCATION`; `usesCleartextTraffic="true"`.

---

## 8. דרישות לא-פונקציונליות (NFR)

### 8.1 ביצועים
- **NFR-001:** בקשות רשת חיצוניות (Desktop) יוגבלו בזמן קצוב (timeout) למניעת נעילת ה-UI — קיים באופן חלקי בלבד (ר' §9, נתיבים ללא timeout).
- **NFR-002:** בקשות מרובות-נקודות (רשת גבהים/טמפרטורה) יפוצלו ל-batches עם השהיות מבוקרות כדי לא לחרוג ממגבלות קצב של ספקי API חיצוניים (Open-Meteo) — ממומש הן ב-Desktop (`/elevation`, `/temp_grid`) והן ב-Android (`fetchElevationGrid`, `fetchTemperatureGrid`).
- **NFR-003:** רינדור heat-image (Android) יוגבל לרזולוציית פלט קבועה (512×512) ללא תלות במספר נקודות הקלט — אך זמן החישוב עצמו (kernel density ל"חום ידני") גדל ליניארית עם מספר הנקודות × פיקסלים (O(n·262144)) ללא אינדוקס מרחבי — ר' §9 לגבי סיכון ביצועים בכמות גבוהה של נקודות.
- **NFR-004:** מסלול טיסה (Desktop ו-Android) יוגבל בגודלו (400 נקודות אחרונות ב-Desktop) כדי להגביל את גודל ה-payload והזמן הנדרש לרינדור.

### 8.2 אמינות וטיפול בשגיאות
- **NFR-010:** כל שגיאת רשת/API תוצג למשתמש בעברית, ולא כ-stack trace גולמי (בשני הצדדים).
- **NFR-011:** כישלון בשכבה משנית (למשל גובה בזמן הצגת מזג אוויר) לא יחסום את הצגת שאר הנתונים (graceful degradation).
- **NFR-012:** מנגנון fallback רב-שכבתי במעקב טיסות (Android) יבטיח זמינות גבוהה יותר של הפיצ'ר גם כאשר מקור מידע בודד חוסם/נכשל (Cloudflare וכו').

### 8.3 שמישות (Usability)
- **NFR-020:** כל טקסט מול המשתמש (תוויות, שגיאות, טיפים) יהיה בעברית בלבד, בכיוון RTL מלא, בשני הצדדים.
- **NFR-021:** פעולות איטיות (טעינת שכבה, חיפוש טיסה/עיר, מיקום GPS) יציגו אינדיקציית טעינה (spinner/טקסט "טוען...") וינעלו את הכפתור הרלוונטי במהלך הביצוע.
- **NFR-022:** היסטוריית קלט (ערים, טיסות) תישמר בין הפעלות ותוצג כקיצורי-דרך לחיצים (chips/dropdown).

### 8.4 אבטחה
- **NFR-030:** מפתחות API וסיסמאות לא יופיעו בבקרת גרסאות (`.gitignore` כולל `.env`) — **בפועל קיימים ליקויים, ר' §9.**
- **NFR-031:** שרתי Flask לא יורצו במצב `debug=True` בסביבת "ייצור" (production-like) — **כרגע כל 3 השרתים רצים כך, ר' §9.**
- **NFR-032:** APK לייצור (release build) ייחתם במפתח ייעודי (לא debug key) — **כרגע לא ממומש, ר' §9.**

### 8.5 תחזוקתיות
- **NFR-040:** לוגיקת חישוב זהה (למשל LOS, Haversine, color-ramp) המיושמת בנפרד בשתי פלטפורמות (ואף פעמיים באותה פלטפורמה) תתועד במפורש כמקור סיכון לאי-עקביות עתידית — ר' §9.
- **NFR-041:** קבצי מקור ה-Android (`maps-gui-android-src`) ייחשבו "מקור האמת" היחיד; שינויים יבוצעו שם בלבד ויועתקו ל-`maps-gui-android` ע"י `build_apk.ps1` (לא לערוך ישירות בתיקיית ה-build).

---

## 9. פערים, קוד מת ובעיות ידועות שהתגלו בסריקה

רשימה זו מתעדת פערים קונקרטיים בין ה"כוונה" המשתמעת בקוד לבין ההתנהגות בפועל, כפי שנצפו בקריאת המקור. אלה **אינם המלצות ביצוע מיידיות** אלא ממצאים לתיעוד, לצורך החלטה מודעת של המשתמש/ מפתח מה לתקן.

| # | ממצא | מיקום |
|---|---|---|
| 1 | Cache מזג אוויר ב-Desktop שומר timestamp אך **אינו אוכף תפוגה (TTL)** — נתונים ישנים יוצגו כ"טריים" ללא הגבלת זמן | `main.py`, `submit_coordinates()` |
| 2 | ולידציית קלט קואורדינטות דוחה `lat==0 or lon==0` כ"לא הוזן", אך (0,0) היא נקודה גיאוגרפית תקפה (מפרץ גינאה) | `main.py`, `save_and_send()` |
| 3 | `flightServerHost` (Android, Tier 1 של מעקב טיסות) אין לו כל דרך הגדרה מה-UI — הקוד קיים אך תמיד ריק, כלומר Tier 1 אינו פעיל בפועל בבנייה הנוכחית | `api_service.dart` |
| 4 | `GET /flight_search` (autocomplete טיסות, Desktop) קיים ומתפקד אך אינו נקרא משום מקום ב-`main.py`/`MAP.py` — פיצ'ר "יתום" | `flight_server.py` |
| 5 | שלושת שרתי Flask רצים עם `debug=True` — חושף debugger אינטראקטיבי ו-reloader; לא מתאים אפילו לשימוש מקומי-בלבד ללא הגנה נוספת | `geo_server.py`, `weather_server.py`, `flight_server.py` |
| 6 | `.env` בשורש הפרויקט מכיל מפתחות/סיסמאות אמיתיים (OpenWeather, Google, FR24) בקובץ טקסט גלוי; מומלץ סבב מפתחות (rotation) אם הקובץ נחשף אי-פעם | `.env` |
| 7 | Token FR24 Gold מוטבע כמחרוזת קשיחה בקוד המקור של ה-Android (`_fr24Token`) — ייחשף בכל APK מבוזר (ניתן לחילוץ ע"י פירוק ה-APK) | `api_service.dart` |
| 8 | APK ה-release חתום במפתח debug (`signingConfigs.getByName("debug")`) ולא במפתח ייעודי לייצור | `maps-gui-android/android/app/build.gradle.kts` |
| 9 | `GET /weather` ב-`weather_server.py` הוא הנתיב היחיד ללא `timeout` על קריאות ה-HTTP החיצוניות — עלול לתלות בקשה ללא הגבלת זמן | `weather_server.py` |
| 10 | קיימים שני מימושים עצמאיים של color-ramp (jet-colormap) בצד Android — אחד ב-`heat_image.dart` (alpha=200) ואחד ב-`map_screen.dart` (`_valueColor`, alpha=255) — סיכון לאי-עקביות ויזואלית אם אחד יעודכן ולא השני | `heat_image.dart`, `map_screen.dart` |
| 11 | אלגוריתם LOS (עקמומיות+רפרקציה) ממומש פעמיים באופן עצמאי — פעם ב-`weather_server.py` (`/los`, Python) ופעם ב-`api_service.dart` (Dart, on-device) — ללא שיתוף קוד; כל שינוי עתידי בנוסחה חייב להתבצע בשני מקומות | `weather_server.py`, `api_service.dart` |
| 12 | `update_bell_icon()` ב-Desktop קיים אך אינו נקרא משום מקום — קוד מת | `main.py` |
| 13 | `enable_heatmap_layer()` (popup "שכבת חום נוספה למפה") קיים אך אינו מקושר לאף כפתור — קוד מת, הוחלף ע"י `toggle_heatmap()` | `main.py` |
| 14 | `apipyqt.py` (fetch_weather_data/fetch_geo_data ואחיהם ה-async) אינו בשימוש בפועל ע"י `main.py` הראשי — `main.py` מבצע קריאות `requests` משלו ישירות; הקובץ נראה כשריד מאיטרציה קודמת (בשימוש ב-`maps-gui-secure` בלבד) | `apipyqt.py` |
| 15 | `LosPoint.fromJson` (Android) קיים אך לא בשימוש בפועל — הבנייה נעשית ישירות מתוצאות החישוב המקומי, לא מ-JSON שרת; שריד ממימוש מוקדם שהתבסס על שרת | `los_session.dart` |
| 16 | `weather_tool.py`/`heatmap_layer.py` מייצרים פלט (`weather_data.csv`, `heatmap.html`) בלתי-תלוי לחלוטין ב"מפה החיה" (`map.html`) — עלול לבלבל בהמשך אם ייחשב "אותה שכבת heatmap" | `weather_tool.py`, `heatmap_layer.py` |
| 17 | תיעוד קודם (`MAPS-GUI_README.md`, מתאריך 19/05/2026) קובע "הפרויקט אינו ב-git repository" — נכון לתאריך כתיבת מסמך זה (07/08/2026) הפרויקט **כן** תחת git (`main` branch, commits קיימים) — יש לעדכן את התיעוד הקודם בהתאם | `MAPS-GUI_README.md` §9 |
| 18 | הקבצים `maps-gui-secure/` (גרסה מקבילה עם חיזוקי אבטחה חלקיים — `html.escape`, `BASE_DIR` יחסי ל-`__file__`, `QThread`-based location worker) ו-`גירסה תקינה/` (snapshot ישן, דצמבר 2024, ללא flight/elevation/LOS) קיימים כענפי-צל לא-מנוהלים ב-git רגיל — מומלץ להבהיר האם מדובר בגיבוי, fork פעיל, או קוד מיועד למיזוג | `maps-gui-secure/`, `גירסה תקינה/` |

---

## 10. תהליך Build ופריסה

מפורט במלואו ב-[MAPS-GUI_README.md §8](MAPS-GUI_README.md#8-build--ייצור-apk) ובסקריפט [build_apk.ps1](build_apk.ps1). תמצית לצורך מפרט זה:
- **Desktop:** `pip install -r requirements.txt` (Python 3.14, venv ב-`.venv/`) + `python main.py`. אין תהליך build/packaging נפרד (מורץ ישירות מהמקור).
- **Android:** `build_apk.ps1` מתקין (במידת הצורך) Java 17, Flutter SDK 3.41.9, Android cmdline-tools + build-tools 35/platform 35, יוצר scaffold Flutter (אם לא קיים), מעתיק `lib/`+`pubspec.yaml`+`AndroidManifest.xml` מ-`maps-gui-android-src/` (מקור האמת) לתוך `maps-gui-android/` (פרויקט ה-build), מריץ `flutter pub get` ו-`flutter build apk --release`. פלט: `maps-gui-android/build/app/outputs/flutter-apk/app-release.apk`.
- ידוע/מתועד: יש להגדיר זיכרון Gradle מספק (`android/gradle.properties`, `org.gradle.jvmargs`) — נצפה כשל build בפועל (`OutOfMemoryError: Metaspace`) כאשר `MaxMetaspaceSize` הוגדר נמוך מדי (256m); הוגדל ל-512m בפרויקט הנוכחי.

---

## 11. נספח: מיפוי קבצים ↔ דרישות

| קובץ | דרישות רלוונטיות |
|---|---|
| `main.py` | FR-D-001 עד FR-D-120 |
| `MAP.py` | FR-D-010 עד FR-D-103 (לוגיקת JS/Leaflet בפועל) |
| `apipyqt.py` | לא בשימוש פעיל (ר' ממצא #14) |
| `geo_server.py` | FR-S-001 עד FR-S-003 |
| `weather_server.py` | FR-S-010 עד FR-S-050 |
| `flight_server.py` | FR-S-060 עד FR-S-063 |
| `weather_tool.py`, `heatmap_layer.py` | כלי עזר עצמאי, לא חלק מזרימת המשתמש הראשית (ר' ממצא #16) |
| `maps-gui-android-src/lib/main.dart` | FR-A-001 |
| `maps-gui-android-src/lib/screens/map_screen.dart` | FR-A-002, FR-A-010–012, FR-A-110 ורוב ה-UI |
| `maps-gui-android-src/lib/state/map_state.dart` | לוגיקת State לכל FR-A-0xx |
| `maps-gui-android-src/lib/services/api_service.dart` | FR-A-020, FR-A-030, FR-A-052, FR-A-060–063, FR-A-080, FR-A-090 |
| `maps-gui-android-src/lib/widgets/controls_panel.dart` | UI תואם ל-FR-A-020/030/050/060 |
| `maps-gui-android-src/lib/utils/heat_image.dart` | FR-A-021, FR-A-040, NFR-003 |
| `maps-gui-android-src/lib/models/*.dart` | מודלים תומכים (LosSession, CityResult, GridPoint) |
| `build_apk.ps1` | §10 |

---

*מסמך זה נוצר ב-07/08/2026 ע"י Claude Code, מבוסס על קריאת קוד המקור בפועל (לא על תיעוד קודם), לרבות `main.py`, `MAP.py`, `apipyqt.py`, שלושת שרתי ה-Flask, וכלל קבצי ה-Dart תחת `maps-gui-android-src/lib/`.*
